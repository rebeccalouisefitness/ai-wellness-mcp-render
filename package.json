{
  "name": "ai-wellness-mcp-render",
  "version": "0.1.0",
  "private": true,
  "description": "AI Wellness MCP server, Render-hosted using @modelcontextprotocol/sdk. Thin protocol wrapper that delegates data operations to the existing Netlify backend.",
  "type": "module",
  "main": "server.js",
  "scripts": {
    "start": "node server.js"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.5.0",
    "express": "^4.21.2",
    "zod": "^3.25.76"
  }
}
